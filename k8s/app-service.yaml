apiVersion: v1
kind: Service

metadata:
  name: employee-app-service
  namespace: employee-management

spec:
  selector:
    app: employee-app

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: NodePort
