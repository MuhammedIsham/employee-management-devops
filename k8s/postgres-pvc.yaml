apiVersion: v1
kind: PersistentVolumeClaim

metadata:
    name: postgres-pvc
    namespace: employee-management

spec:
    accessModes:
        - ReadWriteOnce

    resources:
        requests:
            storage: 2Gi
