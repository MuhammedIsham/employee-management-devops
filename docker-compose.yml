services:
  db:
    image: postgres:16
    container_name: postgres-db
    env_file:
      - .env

  app:
    build: .
    container_name: employee-app

    ports:
      - "5000:5000"

    depends_on:
      - db

    env_file:
      - .env
